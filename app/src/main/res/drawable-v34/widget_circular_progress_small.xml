<?xml version="1.0" encoding="utf-8"?>
<!-- Circular progress for Android 14+ with semantic dynamic colors -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background Ring - SecondaryContainer -->
    <item android:id="@android:id/background">
        <shape
            android:shape="ring"
            android:innerRadiusRatio="3"
            android:thicknessRatio="12"
            android:useLevel="false">
            <solid android:color="@android:color/system_secondary_container_light" />
        </shape>
    </item>

    <!-- Progress Ring with Rounded Caps - Primary -->
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="0"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="0">
            <shape
                android:shape="ring"
                android:innerRadiusRatio="3"
                android:thicknessRatio="12"
                android:useLevel="true">
                <solid android:color="@android:color/system_primary_light" />
                <corners android:radius="2dp" />
            </shape>
        </rotate>
    </item>

</layer-list>
